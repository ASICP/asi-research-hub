#!/bin/bash

# Install dependencies
pip install -r requirements.txt

# Initialize database if it doesn't exist
if [ ! -f "asi_research_hub.db" ]; then
  echo "Initializing database..."
  python database.py
fi

echo "Setup complete!"
